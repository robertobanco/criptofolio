# Comandos para atualizar o GitHub e fazer deploy no Vercel

# 1. Adicionar todos os arquivos modificados
git add .

# 2. Fazer commit das mudanças
git commit -m "Fix: Configurar projeto para deploy no Vercel"

# 3. Enviar para o GitHub
git push origin main

# Nota: Se sua branch principal for 'master' em vez de 'main', use:
# git push origin master
